#!/usr/bin/make -f

export DH_VERBOSE = 1
export PYBUILD_NAME = cortexsec
export PYBUILD_BEFORE_TEST = {interpreter} -m pip install .

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_python3:
	dh_python3 --no-guessing-versions

override_dh_auto_test:
	python3 -m pytest tests/ || true
