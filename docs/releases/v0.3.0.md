# CortexSec v0.3.0 Release

## Highlights
- Native Linux Debian packaging metadata added for `.deb` builds (`debian/`).
- Public namespace remains `cortexsec` with install-time ASCII confirmation.
- APT installation and automatic update guidance added.
- Framework positioning clarified as policy-bounded autonomous and non-destructive.

## Upgrade Notes
- Python package version is now `0.3.0`.
- Git release tag: `v0.3.0`.
